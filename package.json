{
  "name": "contextuse",
  "version": "0.1.0",
  "description": "Configurable ETL library for processing data archives (ChatGPT, Instagram) into structured threads",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build ./src/cli.ts --compile --outfile contextuse",
    "test": "bun test"
  },
  "dependencies": {
    "zod": "^3.23",
    "fflate": "^0.8",
    "stream-json": "^1.9"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7"
  },
  "optionalDependencies": {
    "postgres": "^3.4"
  }
}

