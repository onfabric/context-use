# context-use

## Development

Install the dependencies:

```bash
uv sync
```

Run the project:

```bash
uv run context-use
```

## Type Checking

Run the type checker:

```bash
uv run pyright
```

## Code Style

Lint:

```bash
uv run ruff check --fix
```

Format:

```bash
uv run ruff format
```
